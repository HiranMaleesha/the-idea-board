apiVersion: v1
kind: Service
metadata:
  name: idea-board-backend
  labels:
    app: idea-board
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: idea-board
    component: backend
